# Optional. Without a key the app runs on bundled sample headlines from
# mocks/news.json and shows an "Eksempeldata" badge above the news feed.
# Weather is unaffected — Open-Meteo needs no key.


# Copy this file to .env.local and fill in the value. .env.local is gitignored.
NEWS_API_KEY=your_key_here
